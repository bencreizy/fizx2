// pages/index.js (FINAL PATH CORRECTION)
import React, { useState, useCallback } from 'react';
import Head from 'next/head';
import styled from 'styled-components';

// CORRECTED IMPORTS: Pathing is now correct for your confirmed structure
import ActionButton from '../components/ActionButton'; 
import ProfoundButton from '../components/ProfoundButton'; 
import ParanormalKnob from '../components/ParanormalKnob'; 
import SystemToggle from '../components/SystemToggle'; 
// Also correcting the global CSS import in _app.js to match the confirmed structure:
// import '../styles/globals.css'; (This path is correct for _app.js)

// ... (Rest of the code remains the same as the last clean version) ...

export default function LucaTerminal() {
    // ... (All logic remains the same) ...
    // Note: The structure confirmed in the screenshot is: Root/pages/index.js
    // Therefore, ../components/ is the correct path.
    // ...
}
